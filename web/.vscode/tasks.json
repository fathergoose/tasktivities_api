{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "eslint",
			"problemMatcher": [
				"$eslint-stylish"
			],
			"label": "eslint: lint whole folde r",
			"options": {
				"cwd": "${workspaceFolder}",
				"shell": {
					"args": ["run","lint"],

					"executable": "yarn"
				}
			}
		}
	]
}